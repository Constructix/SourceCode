namespace BuilderPattern
{
    public enum BreadType
    {
        White,
        Wheat
    }

    public enum CheeseType
    {
        American,
        Swiss,
        Cheddar,
        Provolone
    }

    public enum MeatType
    {
        Turkey,
        Ham,
        Chicken,
        Salami
    }
}